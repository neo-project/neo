name: Release (nuget)

# Trigger the workflow on a release event when a new release is published
on:
  release:
    types: [published]

# Define environment variables
env:
  DOTNET_VERSION: 8.0.x
  CONFIGURATION: Release

jobs:
  nuget-release:
    runs-on: ubuntu-latest
    steps:
    # Step to set the application version from the release tag
    - name: Set Application Version (Environment Variable)
      run: |
        APP_VERSION=$(echo '${{ github.event.release.tag_name }}' | cut -d 'v' -f 2)
        echo "APP_VERSION=$APP_VERSION" >> $GITHUB_ENV

    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: LevelDb (win-x64)
      run: |
        mkdir -vp ./bin/runtimes/win-x64/native
        mkdir -vp ./bin/runtimes/win-arm64/native
        cd ./bin/runtimes/win-x64/native
        wget https://github.com/neo-ngd/leveldb/releases/download/v1.23/libleveldb-win-x64.zip
        unzip libleveldb-win-x64.zip
        mv -v ./libleveldb-win-x64/libleveldb.dll .
        ls -la

    - name: LevelDb (win-arm64)
      run: |
        cd ./bin/runtimes/win-arm64/native
        wget https://github.com/neo-ngd/leveldb/releases/download/v1.23/libleveldb-win-arm64.zip
        unzip libleveldb-win-arm64.zip
        mv -v ./libleveldb-win-arm64/libleveldb.dll .
        ls -la

    - name: Pack NuGet Packages
      run: |
        dotnet pack ./neo.sln \
        --configuration Release \
        --output ./bin \
        --verbosity normal \
        -p:VersionPrefix=${{ env.APP_VERSION }}

    - name: Publish to NuGet.org
      run: |
        dotnet nuget push ./bin/*.nupkg \
        --source https://api.nuget.org/v3/index.json \
        --api-key ${{ secrets.NUGET_TOKEN }} \
        --skip-duplicate
