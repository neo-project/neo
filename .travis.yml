language: csharp

os:
 - linux
 - osx

mono: none
dotnet: 2.0.0

before_install:
  - cd neo.UnitTests
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then ulimit -n 2048; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update; fi

script:
 - dotnet restore
 - dotnet test
